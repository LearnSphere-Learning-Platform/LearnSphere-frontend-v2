# Copy this file to .env.e2e and fill in real accounts from your dev database.
# These unlock the login/role-gated tests in e2e/protected-routes.spec.js -
# any test that needs a credential pair is SKIPPED (not failed) if it's unset,
# so the suite runs fine without this file too, just with fewer tests.
#
# Load before running tests, e.g. (bash):
#   export $(grep -v '^#' .env.e2e | xargs) && npm run test:e2e

E2E_STUDENT_EMAIL=
E2E_STUDENT_PASSWORD=

E2E_INSTRUCTOR_EMAIL=
E2E_INSTRUCTOR_PASSWORD=

E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=
