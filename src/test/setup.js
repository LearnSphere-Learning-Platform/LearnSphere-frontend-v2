// Vitest setup file (src/test/setup.js) - runs before every test file.
// Adds jest-dom's DOM matchers (toBeInTheDocument, toHaveTextContent, etc.) to Vitest's expect.
import '@testing-library/jest-dom';
